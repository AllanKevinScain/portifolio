import type { AnimationPlayerInterface } from "@/types";

export const AnimationPlayer: React.FC<AnimationPlayerInterface> = (props) => {
  const { style, ...lottieRestProps } = props;

  return null;
};
